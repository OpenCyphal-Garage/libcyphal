[
    {
        "name": "toolshed-gcc",
        "description": "Compile and run tests using the toolshed's default gcc toolchain.",
        "toolchainFile": "${workspaceFolder}/.devcontainer/cmake/toolchains/gcc-native.cmake",
        "cmakeSettings": {
            "CETLVAST_FLAG_SET": "${workspaceFolder}/cmake/compiler_flag_sets/default.cmake"
        }
    },
    {
        "name": "toolshed-clang",
        "description": "Compile and run tests using the toolshed's default clang toolchain.",
        "toolchainFile": "${workspaceFolder}/.devcontainer/cmake/toolchains/clang-native.cmake",
        "cmakeSettings": {
            "CETLVAST_FLAG_SET": "${workspaceFolder}/cmake/compiler_flag_sets/default.cmake"
        }
    }
]
